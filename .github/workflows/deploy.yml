name: Deploy WordPress Plugin

on:
  push:
    tags:
      - '*'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install SVN
        run: |
          sudo apt-get update
          sudo apt-get install -y subversion rsync

      - name: Checkout WordPress SVN
        env:
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
        run: |
          svn checkout https://plugins.svn.wordpress.org/dashclean svn \
          --username "$SVN_USERNAME" \
          --password "$SVN_PASSWORD" \
          --no-auth-cache \
          --non-interactive

      - name: Deploy plugin
        env:
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
        run: |
          VERSION=${GITHUB_REF_NAME}

          rsync -av --delete \
          --exclude '.git' \
          --exclude '.github' \
          ./ svn/trunk/

          rsync -av ./assets/ svn/assets/

          cd svn

          svn add --force . --auto-props --parents --depth infinity -q

          svn commit -m "Deploy version $VERSION" \
          --username "$SVN_USERNAME" \
          --password "$SVN_PASSWORD" \
          --no-auth-cache \
          --non-interactive

          svn copy trunk tags/$VERSION

          svn commit -m "Tagging version $VERSION" \
          --username "$SVN_USERNAME" \
          --password "$SVN_PASSWORD" \
          --no-auth-cache \
          --non-interactive
